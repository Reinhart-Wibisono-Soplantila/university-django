from django.urls import path
# from .views import ScheduleApiView

app_name="app_schedule"

urlpatterns = [
    # path("", ScheduleApiView.as_view(), name='all_schedule'),
    # path("<int:schedule_id>", ScheduleApiView.as_view(), name='specific_schedule')
]
